import React from "react";
import classes from './Toolbar.module.css'

const ToolbarWrapper = (props) => {

  return (
    <div className={classes.ToolbarWrapper}>
        <h1>ToDO React List</h1>
    </div>
  )
};

export default ToolbarWrapper;